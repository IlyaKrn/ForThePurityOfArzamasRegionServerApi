# ForThePurityOfArzamasRegionServerApi

## Описание
Сервер для <a href="https://github.com/IlyaKrn/ForThePurityOfArzamasRegion/">мобильного приложения</a> "За чистоту Арзамасского края!", созданный как альтернатива Firebase</br>
Для хранения данных используется <a href="https://www.postgresql.org/">PostgreSQL</a>
## Модели данных
Для отправки **от сервера к клиенту** данных об объектах, хранящихся в базе данных, используются классы из пакета Domain.Models.Data.ResponseModels, помещённые в ResponseModel</br></br>
Для отправки **от клиента к серверу** данных об объектах, данные о которых необхдимо сохраненить, используются классы из пакета Domain.Models.Data.RequestModels</br></br>
Для **хранения данных в бд** используются классы из пакета , используются классы из пакета Domain.Models.Data.DatabaseModels</br></br>
## GET method/users.getListAll
### Возврат
Возвращает данные всех пользователей
## GET method/users.getListById
### Параметры
**'user_ids'** содержит идентификаторы пользователей, даные о которых необхдимо получить
### Возврат
Возвращает данные запрашиваемых пользователей. Если данных о пользователе нет, вернет **null**
## GET method/users.getListByEmail
### Параметры
**'email'** содержит **email**, по которому производится поиск поьзователей
### Возврат
**Возвращает** данные пользователей с указанным **email**
## POST method/users.setById
### Параметры
**'user_id'** содержит идентификатор пользователя, даные о которых необхдимо изменить
### Тело запроса
Словарь, где ключ - параметр пользователя, а значение - новое значение параметра
### Возврат
Возвращает измененные данные пользователя
## POST method/users.create
### Тело запроса
Пользователь, которого нужно создать
### Возврат
Возвращает сохраненные данные пользователя
## POST method/users.delete
### Параметры
**'user_id'** содержит идентификатор пользователя, даные о которых необхдимо удалить
### Возврат
Возвращает null


## GET method/projects.getListAll
### Возврат
Возвращает данные всех проектов
## GET method/projects.getListById
### Параметры
**'project_ids'** содержит идентификаторы проектов, даные о которых необхдимо получить
### Возврат
Возвращает данные запрашиваемых проектов. Если данных о проекте нет, вернет **null**
## POST method/projects.setById
### Параметры
**'project_id'** содержит идентификатор проекта, даные о которых необхдимо изменить
### Тело запроса
Словарь, где ключ - параметр проекта, а значение - новое значение параметра
### Возврат
Возвращает измененные данные проекта
## POST method/projects.create
### Тело запроса
Проект, который нужно создать
### Возврат
Возвращает сохраненные данные проекта
## POST method/projects.delete
### Параметры
**'project_id'** содержит идентификатор проекта, даные которые необхдимо удалить
### Возврат
Возвращает null

## GET method/events.getListAll
### Возврат
Возвращает данные всех публикаций
## GET method/events.getListById
### Параметры
**'event_ids'** содержит идентификаторы публикаций, даные о которых необхдимо получить
### Возврат
Возвращает данные запрашиваемых публикаций. Если данных о проекте нет, вернет **null**
## POST method/events.setById
### Параметры
**'event_id'** содержит идентификатор публикации, даные о которых необхдимо изменить
### Тело запроса
Словарь, где ключ - параметр публикации, а значение - новое значение параметра
### Возврат
Возвращает измененные данные публикации
## POST method/events.create
### Тело запроса
Публикация, которую нужно создать
### Возврат
Возвращает сохраненные данные публикации
## POST method/events.delete
### Параметры
**'event_id'** содержит идентификатор публикации, даные которые необхдимо удалить
### Возврат
Возвращает null


## GET method/locates.getListAll
### Возврат
Возвращает данные всех публикаций
## GET method/locates.getListById
### Параметры
**'locate_ids'** содержит идентификаторы меток, даные о которых необхдимо получить
### Возврат
Возвращает данные запрашиваемых меток. Если данных о проекте нет, вернет **null**
## POST method/locates.setById
### Параметры
**'locate_id'** содержит идентификатор публикации, даные о которых необхдимо изменить
### Тело запроса
Словарь, где ключ - параметр метки, а значение - новое значение параметра
### Возврат
Возвращает измененные данные публикации
## POST method/locates.create
### Тело запроса
Метка, которую нужно создать
### Возврат
Возвращает сохраненные данные метки
## POST method/locates.delete
### Параметры
**'locate_id'** содержит идентификатор метки, даные которые необхдимо удалить
### Возврат
Возвращает null

